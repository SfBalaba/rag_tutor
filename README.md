# Пайплайн подготовки данных для RAG-системы

## Цель
Преобразовать неструктурированные учебные материалы (PDF, DJVU) в структурированные, легко обрабатываемые Markdown-файлы, готовые для построения RAG-системы (Retrieval-Augmented Generation).

---

## 🧰 Используемые инструменты

| Компонент | Назначение |
|----------|-----------|
| **LlamaParse** | Высокоточный парсинг PDF с поддержкой формул (в LaTeX), таблиц, заголовков и русского текста |
| **ddjvu / Calibre (ebook-convert)** | Конвертация DJVU → PDF |
| **sentence-transformers** | Генерация эмбеддингов (`intfloat/multilingual-e5-large`) |
| **FAISS** | Эффективное хранение и поиск векторов |
| **Google Colab + Google Drive** | Вычисления и хранение |
| **nest_asyncio** | Поддержка асинхронного LlamaParse в Colab |
| **tqdm / pathlib / re** | Прогресс, работа с путями и обработка текста |



---

## 🚀 Инструкция по запуску

### Требования
- Python ≥ 3.9
- Доступ в интернет (для LlamaParse и установки пакетов)
- Бесплатный API-ключ от [LlamaCloud](https://cloud.llamaindex.ai) (1000 страниц/месяц бесплатно)

---

### 🔧 Общая подготовка (для всех ОС)

1. **Создайте проект и активируйте виртуальное окружение**:
 ```bash
 python -m venv rag-env
 source rag-env/bin/activate    # Linux/macOS
 # или
 rag-env\Scripts\activate       # Windows
 
```

```
pip install -U pip
pip install -r requirements.txt
pip install llama-parse tqdm
```
### 🔧 Для Linux
 ```bash
sudo apt update
sudo apt install -y djvulibre-bin
sudo apt install tesseract-ocr tesseract-ocr-rus poppler-utils djvulibre-bin
pip install pymupdf pdf2image pytesseract
ddjvu --version  # Должно показать версию DjVuLibre
```
2. **Запустите скрипт:**:
 ```bash
python scripts/parse_to_md_and_chunks_fais.py
```

### 🪟 Windows
#### Вариант 1: WSL (рекомендуется)
#####  Нативно (без WSL)
 - Установите Python:
- Скачайте с python.org
- При установке отметьте "Add Python to PATH"
- Установите DjVuLibre для Windows:
- Скачайте установщик: DjVuLibre Windows
 - Запустите .exe и следуйте инструкциям.
- Добавьте папку установки в PATH (обычно C:\Program Files\DjVuLibre\bin):
- Нажмите Win + R → введите sysdm.cpl → "Дополнительно" → "Переменные среды"
- В "Системные переменные" найдите Path → "Изменить" → "Создать" → добавьте путь.
###### Проверьте в PowerShell:
```powershell
ddjvu --version
```
###### Запустите скрипт в PowerShell:
```powershell
python scripts/parse_to_md_and_chunks_fais.py
```
⚠️ Важно: нативный Windows может вызывать проблемы с кодировкой и путями. WSL — надёжнее.


### Результат
**После завершения вы получите:**
```
├── parsed_full/        # Полные Markdown-версии учебников
├── parsed_chunks/      # чанки по темам (для RAG)
└── faiss_db/
    ├── faiss.index     # Векторный индекс (FAISS)
    ├── metadata.jsonl  # Метаданные чанков
    └── embeddings.npy  # Опционально: сырые эмбеддинги
```

## Этапы пайплайна

### 1. Сбор исходных документов
- **Форматы**: `.pdf`, `.djvu`
- **Структура**:
```
data/
├── elementary/
├── middle_school/
├── high_school/
└── university/
```

- **Содержание**: учебники, задачники, методички (в т.ч. с формулами, таблицами, схемами).

> **Зачем**: RAG требует "питаться" качественным, релевантным контентом. Учебники — идеальный источник для образовательного ИИ.

---

### 2. Конвертация DJVU → PDF (если нужно)
- Используется утилита `ddjvu` (из пакета `djvulibre-bin`).
- Таймаут увеличен до **10 минут** для больших файлов (напр., учебник Атанасяна).

> **Зачем**: LlamaParse принимает только PDF. DJVU — устаревший, но компактный формат, популярный в сканах.

---

### 3. Извлечение структурированного текста с помощью LlamaParse
- **Инструмент**: облачный парсер [LlamaParse](https://docs.llamaindex.ai/en/latest/module_guides/loading/llamaparse/)
- **Особенности**:
- Сохраняет **заголовки**, **абзацы**, **списки**.
- Преобразует **математические формулы** → **LaTeX** (`$...$`, `$$...$$`).
- Преобразует **таблицы** → **Markdown-таблицы**.
- Указывает **изображения** через placeholder’ы (`![Изображение](...)`).
- Поддержка **русского языка**, **латиницы**, **греческих букв**, **римских цифр**.
- **Промпт**: явно указывает требования к качеству вывода.

> **Зачем**: Обычные OCR-системы (Tesseract) теряют структуру и формулы. LlamaParse — специализированный ИИ-парсер для технических документов.

---

### 4. Сохранение полного Markdown-документа
- Путь: `parsed_full/middle_school/Атанасян_7-9.md`
- Сохраняется **точная структура папок** как в исходных данных.

> **Зачем**: Для ручной проверки, резервного копирования, последующей постобработки или визуализации.

---

### 5. Разбиение документа на чанки по заголовкам
- Каждый заголовок (`#`, `##`, ...) становится началом нового чанка.
- Пример:

```
parsed_chunks/middle_school/Атанасян_7-9/
├── 000_Начальные_геометрические_сведения.md
├── 001_Треугольники.md
└── 002_Параллельные_прямые.md
```


- Каждый чанк содержит:
- Заголовок,
- Связанный текст,
- Формулы (в LaTeX),
- Таблицы,
- Указания на изображения.

> **Зачем**: RAG работает с **контекстными фрагментами** (чанками). Чанки по темам — идеальны для точного поиска: запрос *"свойства параллелограмма"* вернёт именно раздел про параллелограммы, а не весь учебник.

---

### 6. Санитизация имён файлов
- Удаление недопустимых символов (`/`, `:`, `*` и т.д.).
- Замена пробелов на `_`.

> **Зачем**: Обеспечивает совместимость с файловой системой и избегает ошибок при чтении.

---

###  7. Иерархическая чанкизация
- Разбивка по заголовкам (#, ##, ...)
- Пустые чанки (например, заголовок без текста) автоматически отбрасываются
- Каждый чанк содержит полный путь:
["Алгебра", "Функции", "Область определения"]

---

###  8.  Метаданные
Каждый чанк обогащается:

- level: elementary / middle_school / high_school / university
- grade: имя папки (например, 10_klass)
- book_title: имя файла без расширения
- source_file: относительный путь
- heading_path: иерархия заголовков
- chunk_id: уникальный ID
###  9.  Векторизация и сохранение
- Эмбеддинги генерируются моделью intfloat/multilingual-e5-large
- Векторы сохраняются в FAISS Index
- Метаданные — в metadata.jsonl (один JSON-объект на строку)
- Полные Markdown-файлы — для отладки

### Результаты
Скрипт создаёт в Google Drive:
```
drive/MyDrive/try_2/
├── parsed_full/        # Полные Markdown-версии учебников
├── parsed_chunks/      # Чанки по каждому документу (для отладки)
└── faiss_db/
    ├── faiss.index     # Векторный индекс (FAISS)
    ├── metadata.jsonl  # Метаданные чанков
    └── embeddings.npy  # Опционально: сырые эмбеддинги
```

## Как это используется дальше в RAG?

### 1. Загрузите faiss.index и metadata.jsonl в вашу RAG-систему.
При получении запроса:
 - Сгенерируйте эмбеддинг с помощью intfloat/multilingual-e5-large (префикс "query: ")
 - Выполните поиск в FAISS
 - Получите релевантные чанки + метаданные
 - Передайте контекст + запрос в LLM (Llama 3, GigaChat и др.)
>💡 Благодаря метаданным вы можете фильтровать результаты: 
> «Покажи только формулы из 10 класса» или «Ответь на уровне университета».

### 2. Поиск (Retrieval)
- При запросе пользователя (напр., *"Как найти площадь трапеции?"*):
- запрос эмбеддится,
- ищутся **топ-K ближайших чанков** в векторной БД,
- возвращаются **релевантные фрагменты** (с формулами в LaTeX!).

### 3. Генерация (Augmentation)
- Найденные чанки + исходный запрос передаются в **LLM** (Llama 3, GPT-4 и т.д.).
- LLM генерирует **точный, обоснованный ответ**, ссылаясь на учебник:
> Площадь трапеции:  
> **S = ½ · (a + b) · h**  
> где **a**, **b** — основания, **h** — высота.  
> Источник: `5_класс/Атанасян, §53`

### 4. Интеграция
- Готовый RAG-движок можно встроить в:
- чат-бот для школьников,
- систему автоматической проверки решений,
- репетитора на базе ИИ.

---

## Преимущества пайплайна

| Аспект | Выгода |
|-------|--------|
| **Формулы в LaTeX** | Отображаются корректно в вебе, поддерживаются LLM |
| **Чанки по темам** | Высокая точность поиска, минимум "шума" |
| **Поддержка русского** | Релевантные ответы на родном языке |
| **Изображения (placeholder’ы)** | Возможность в будущем добавить мультимодальность |
| **Масштабируемость** | Легко добавить новые учебники или уровни |

---

## Итог
🏁 Итог
Скрипт превращает неструктурированные учебники в готовую к использованию RAG-базу знаний:   

- текст структурирован,
- формулы в LaTeX,
- чанки информативны и не содержат мусора,
- векторы и метаданные позволяют строить точные и контекстно-зависимые ответы.