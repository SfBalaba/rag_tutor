# Math Tutor Agent

AI-powered math tutor with code interpreter capabilities. Backend на FastAPI, интерфейс на Streamlit.

## Возможности

- 💬 Интерактивный чат с AI-репетитором
- 🐍 Выполнение Python кода для вычислений
- 📊 Построение графиков и визуализация
- 📚 Поиск информации в учебниках (RAG)

## Быстрый старт

### 1. Установка зависимостей

```bash
# Установите uv (если еще не установлен)
curl -LsSf https://astral.sh/uv/install.sh | sh

# Установите зависимости
uv pip install ".[app]"
```

### 2. Загрузка данных RAG

```bash
# Автоматическая загрузка
pip install gdown
python scripts/download_faiss_data.py
```

Или вручную: скачайте из [Google Drive](https://drive.google.com/drive/folders/1dyrnFUngeg-koqz8M1G21zZMmR5D_Ig8) и распакуйте в `data/`:
- `faiss_db.zip` → `data/faiss_db/`
- `all_chunks_with_meta_all.pickle` → `data/`

### 3. Настройка переменных окружения

Создайте `.env` в корне проекта:

```env
OPENROUTER_API_KEY=your-openrouter-api-key-here
OPENROUTER_MODEL=openai/gpt-4o-mini
E2B_API_KEY=your-e2b-api-key-here
```

### 4. Запуск

**Docker (рекомендуется):**
```bash
docker-compose up --build
```
- Backend: http://localhost:8001
- Frontend: http://localhost:8502

**Локально:**
```bash
# Терминал 1: Backend
cd app
python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000

# Терминал 2: Frontend
streamlit run app/frontend/streamlit_app.py
```

## Примеры вопросов

- "Объясни, что такое производная функции"
- "Как решать квадратные уравнения? Покажи на примере"
- "Построй график функции y = sin(x)"
- "В чём смысл интеграла? Объясни простыми словами"

## Структура проекта

```
app/
├── backend/          # FastAPI сервер и логика агента
├── frontend/         # Streamlit интерфейс
└── Dockerfile

data/                # Данные RAG (нужно скачать)
├── faiss_db/
└── all_chunks_with_meta_all.pickle
```

## Troubleshooting

**Ошибка загрузки RAG данных:**
- Проверьте, что файлы находятся в `data/faiss_db/` и `data/`
- Запустите `python scripts/download_faiss_data.py` заново

**Ошибка подключения к API:**
- Убедитесь, что FastAPI сервер запущен
- Проверьте переменные окружения в `.env`
